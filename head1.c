# include<stdlib.h>
# include<stdio.h>
# include<time.h>

int main()
{
    // // 随机函数
    // // 1. 设置随机种子 -- 与时间联用
    // srand(time(NULL));
    // // 2. 取随机数
    // int num = rand();
    // printf("%d\n", num);
    // exit(0); // 0 - 正常退出应用程序
    // return 0;

    printf("这是普通文字");
    system("color 4E");
    return 0;
}